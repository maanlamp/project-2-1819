<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<meta http-equiv="X-UA-Compatible" content="ie=edge">
	<link rel="stylesheet" href="css/viewport.css">
	<link rel="stylesheet" href="css/odometer-minimal.css">
	<link rel="stylesheet" href="css/overview.css">
	<link href="https://fonts.googleapis.com/css?family=Roboto:300,400,500,900" rel="stylesheet">
	<script src="js/odometer.min.js" defer></script>
	<title>Overzicht</title>
</head>
<body>
	<main id="app" class="<%= tmpstr %>">
		<header>
			<img id="sun" src="images/sun.svg" alt="Schijnend zonnetje.">
			<h1>Gem. temperatuur</h1>
			<p id="avgtmp"><%= avgtmp; %></p>
			<p><span id="free"><%= free; %></span> <%= (free === 1) ? "kamer" : "kamers"; %> vrij</p>
		</header>
		<div id="rooms">
			<% rooms.forEach(room => { %>
				<% const classlist = "room"
					+ ((room.occ) ? " occ" : "")
					+ ((room.bkd) ? " bkd" : "");
				%>
				<a class="<%= classlist; %>" href=<%= `/${room.name}`;%>>
					<h2><%= room.name; %></h2>
					<p><%= room.tmp; %></p>
				</a>
			<% }); %>
		</div>
	</main>
</body>
</html>